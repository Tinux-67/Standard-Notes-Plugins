import { SnMediatorOptions } from './sn-types';
declare class StandardNotesExtensionAPI {
    private contentWindow;
    private component;
    private sentMessages;
    private messageQueue;
    private lastStreamedItem?;
    private pendingSaveItems?;
    private pendingSaveTimeout?;
    private pendingSaveParams?;
    private coallesedSavingDelay;
    private messageHandler?;
    private onThemesChangeCallback?;
    private subscriptions;
    private generateNotePreview;
    initialize(options?: SnMediatorOptions): void;
    subscribe(callback: (note: string, meta: any) => void): () => void;
    get text(): string;
    get meta(): any;
    get extensionMeta(): any;
    get locked(): boolean;
    get preview(): string;
    set text(newText: string);
    set preview(newPreview: string);
    set meta(newMeta: any);
    set extensionMeta(data: any);
    private registerMessageHandler;
    private handleMessage;
    private onReady;
    get isRunningInDesktopApplication(): boolean;
    get isRunningInMobileApplication(): boolean;
    get isRunningInBrowser(): boolean;
    private postMessage;
    private activateThemes;
    private themeElementForUrl;
    private deactivateTheme;
    private generateUUID;
    get platform(): string | undefined;
    get environment(): string | undefined;
    private performSavingOfItems;
    private saveNote;
    private jsonObjectForItem;
    private checkNoteExists;
}
export declare const snApi: StandardNotesExtensionAPI;
export default snApi;
